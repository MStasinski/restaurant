
<div class="row">
  {{#each  sortedModel as |alforno|}}
    <div class="col-md-12">
      <div class="panel panel-default library-item">
      
        <div class="panel-heading">
          <h3 class="panel-title">ID: {{alforno.rank}}&nbsp;&nbsp;&nbsp;&nbsp; {{alforno.name}}</h3>
        </div>

        <div class="panel-body">
          <h3 class="panel-title">Cena: {{alforno.price}}</h3>
        </div>

        <div class="panel-body">
          <p>opis: {{alforno.desc}}</p>
        </div>

        <div class="panel-footer text-right">
          {{#link-to 'alfornos.edit' alforno.id class='btn btn-success btn-xs'}}Edytuj{{/link-to}}
          <button class="btn btn-danger btn-xs" {{action 'deleteLibrary' alforno}}>Usuń</button>
        </div>
      </div>
    </div>
  {{/each}}
</div>
